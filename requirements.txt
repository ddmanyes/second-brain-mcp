# Floors bumped 2026-06-10 to current tested versions (173 tests green).
mcp[cli]>=1.27.2
markitdown[all]>=0.1.6
duckdb>=1.5.3
playwright>=1.60.0
markdown2>=2.5.5
requests>=2.34.2
anthropic>=0.109.1
ollama>=0.6.2
# markitdown[all] pins magika~=0.6.1, but we override to >=1.0.0 for Python 3.14
# support (matches pyproject [tool.uv] override-dependencies). markitdown works
# with magika 1.0.3 in practice; the `pip check` warning is expected and benign.
magika>=1.0.0
